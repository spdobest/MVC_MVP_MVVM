<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="spm.mvc_mvp_mvvm">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DesignPatternActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.mvc.MvcMainActivity">
        </activity>
        <activity android:name=".ui.mvp.MvpMainActivity">
        </activity>
        <activity android:name=".ui.mvvm.MvvmMainActivity">
        </activity>
        <activity android:name=".ui.mvp.MvpLoginActivity">
        </activity>
        <activity android:name=".architecturComponent.ComponentActivity">
        </activity>
    </application>

</manifest>